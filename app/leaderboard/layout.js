export const metadata = {
  title: 'Leaderboard — CivicWatch',
  description: 'The most active stock traders in the U.S. Congress.',
}

export default function LeaderboardLayout({ children }) {
  return children
}
