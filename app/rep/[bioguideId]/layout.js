export const metadata = {
  title: 'Representative Profile — CivicWatch',
  description: 'View votes, trades, net worth, and legislative activity for this member of Congress.',
}

export default function RepLayout({ children }) {
  return children
}
