export const metadata = {
  title: 'Go Pro — CivicWatch',
  description: 'Unlock full AI accountability reports, trade conflict analysis, wealth trajectories, real-time alerts, and local representative lookup with CivicWatch Pro.',
}

export default function ProLayout({ children }) {
  return children
}
